    // compare = min((a - 1) % 10, (b - 1) % 10);
    // compare = min(compare, (c - 1) % 10);
    // compare = min(compare, (d - 1) % 10);
    // compare = min(compare, (e - 1) % 10);
    // compare++;