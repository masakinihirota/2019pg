

#入力
x軸が横
y軸が縦
 rep(y, h) rep(x, w) cin >> c[x][y];

#dy, dx
座標上を探索する時に便利。

int dy[]={0, 0, 1, -1};
int dx[]={1, -1, 0, 0};

int main(){
   int ny, nx; //今いる座標。
   REP(i, 4){
      int ty=ny+dy[i];
      int tx=nx+dx[i]; //探索対象にする座標。
   }
}
斜めに移動できるとかいう条件があればその都度足していけば良い。
