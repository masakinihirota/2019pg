座標圧縮とは
8, 100, 33, 12, 6
のような数値があったときに、それぞれの数値に対して、
それが何番目に小さい数値なのかを割り振る作業。
1, 4, 3, 2, 0

vals.erase(unique(vals.begin(), vals.end()), vals.end());

std::uniqueはゴミの手前のポインタを返すので、
vector::eraseで後ろのゴミを削除する。
これで重複要素のないvectorが得られる。

座標圧縮問題。
ABC113C
